# Сборка web (vite build / CI). Браузер ходит на тот же хост, nginx проксирует /api.
# Старый сплит на 192.0.2.18 ломал cookie: страница на :3002, API на другом IP → 401 → /auth.
VITE_API_URL=/api
VITE_FILE_URL=/api
VITE_PARTNER_CREATE_RESTRICTED=true
