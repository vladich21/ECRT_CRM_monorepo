/** Совпадает с историческим ключом в коде (общий кэш со статусами патентов - см. patentStatusQueryKeys). */
export const patentIntellectPropsQueryKeys = {
  all: ['patent-statuses'] as const,
} as const;
